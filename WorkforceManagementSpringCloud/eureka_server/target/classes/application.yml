server:
  port: 8888
spring:
  application:
    name: eureka-server
#单机版配置
eureka:
  client:
    fetch-registry: false #是否需要从Eureka获取注册信息
    register-with-eureka: false #是否把服务注册到Eureka
    service-url: #暴露eureka注册地址
      defaultZone: http://127.0.0.1:9999/eureka

